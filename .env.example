# =============================================================================
# Content Hub Frontend - Environment Variables
# =============================================================================
# Copy this file to `.env.local` for local development.
# For production, set these via CI/CD environment variables or `.env.production.local`.
#
# All variables must be prefixed with VITE_ to be exposed to the client-side code.
# See: https://vite.dev/guide/env-and-mode
# =============================================================================

# Backend API base URL
# - Local development: http://localhost:8080 (proxied via Vite dev server, see vite.config.ts)
# - Production: your production backend domain (e.g., https://api.example.com)
VITE_API_BASE_URL=

# API Key for authenticating with the Content Hub backend
# The backend expects this in the `X-API-Key` header.
# NEVER commit real API keys — use .env.local or CI/CD secrets.
VITE_API_KEY=
